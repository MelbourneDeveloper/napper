# POST with JSON body — verify echo
[meta]
name = "httpbin POST echo"
tags = ["httpbin", "write"]

[request]
method = POST
url = {{baseUrl}}/post

[request.headers]
Content-Type = application/json
Accept = application/json

[request.body]
content-type = application/json
"""
{
  "tool": "nap",
  "version": "0.1.0"
}
"""

[assert]
status = 200
headers.Content-Type contains "json"
body.json exists
